import {
  EMPLOYEELOGIN_ERROR,
  EMPLOYEELOGIN_SUCCESS,
  EMPLOYEE_ENDSESION,
} from '../../types';

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
